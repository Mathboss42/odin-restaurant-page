const template = `
    <div class="container">
                
    </div>
`;

export function setContact() {
    const element = document.querySelector('.content');

    element.innerHTML = template;
}